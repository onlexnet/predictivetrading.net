ENV_APP_PORT=${APP_PORT}


spring.main.banner-mode=off

spring.threads.virtual.enabled=true

grpc.serverPort=${ENV_APP_PORT}

# default: true
spring.docker.compose.skip.in-tests=false

server.shutdown=graceful