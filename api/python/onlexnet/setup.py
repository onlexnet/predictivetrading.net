from setuptools import setup, find_packages

setup(
    name="onlexnet",
    author="siudeks",
    description="Helper methods to support Python coders in predictivetrading.net",
    version="0.0.25",
    packages=find_packages())
